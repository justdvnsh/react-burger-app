const wrap = (props) => props.children;


// made a wrapper file, because was facing some issues saving aux.js file.
// but it works the same way.
// takes props as arguments and returns it as it is.
export default wrap;
